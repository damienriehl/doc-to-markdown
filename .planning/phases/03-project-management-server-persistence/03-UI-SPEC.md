---
phase: 3
slug: project-management-server-persistence
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-17
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Phase 3: Project Management + Server Persistence.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-rolled CSS variables, inline styles throughout |
| Preset | not applicable |
| Component library | none (no Radix, no shadcn) |
| Icon library | Inline SVG paths or Unicode symbols — no new library (constraint: no new npm deps) |
| Font (body) | 'Source Sans 3', 'Source Sans Pro', system-ui, sans-serif |
| Font (mono) | 'IBM Plex Mono', 'SF Mono', monospace |

Source: detected from `src/App.jsx` CSS variable declarations (lines 1450–1459) and `src/index.css`.

---

## CSS Variables (existing — do not redefine)

All new components in this phase MUST use these variables exclusively. Do not hard-code color values except where noted in the Color section below.

| Variable | Value | Semantic Use |
|----------|-------|-------------|
| `--bg` | `#faf9f7` | Page background, surface, input backgrounds |
| `--card` | `#ffffff` | Card surfaces |
| `--border` | `#e5e2dc` | All borders, dividers |
| `--text` | `#1a1814` | Primary text |
| `--muted` | `#8a8478` | Secondary text, metadata, placeholders |
| `--accent` | `#2563eb` | Accent (see reserved list below) |
| `--accent-dim` | `#dbeafe` | Hover tints over accent items |
| `--accent-bg` | `#eff6ff` | Background tint for active/selected states |
| `--font-body` | `'Source Sans 3', ...` | All non-code text |
| `--font-mono` | `'IBM Plex Mono', ...` | Badges, status indicators, file names |

Source: `src/App.jsx` lines 1450–1459, confirmed in `src/ProjectList.jsx` and `src/index.css`.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Button icon gaps, badge internal padding, inline icon spacing |
| sm | 8px | Gap between sibling buttons, card header internal spacing |
| md | 12px | Project card padding (established: `padding: "10px 12px"`) |
| md+ | 16px | Modal internal padding sections, section gaps |
| lg | 24px | Modal padding (established: `padding: 24`), main layout padding |
| xl | 32px | Page top padding (established: `padding: "32px 24px"`) |

Source: Extracted from existing `src/App.jsx` and `src/ProjectList.jsx` spacing values.

Exceptions:
- Hover-action icon buttons: 28px minimum touch target (inline 28×28px) — desktop-only tool, no mobile 44px requirement
- Server status dot: 8×8px circle — purely visual indicator, not interactive touch target
- Rename input: fills project name slot exactly — inherits card padding, no extra margin

---

## Typography

| Role | Size | Weight | Line Height | Font | Usage |
|------|------|--------|-------------|------|-------|
| Body | 13px | 400 | 1.5 | `--font-body` | Modal body text, metadata descriptions, empty state copy |
| Label | 14px | 600 | 1.3 | `--font-body` | Project card names, inline rename input |
| Heading | 16px | 700 | 1.2 | `--font-body` | Modal title (h3) |
| Display | 22px | 700 | 1.1 | `--font-body` | App h1 "RAG Converter" (existing — do not change) |

Source: Extracted from existing `src/App.jsx` fontSizes: 13 (body/muted), 14 (card name), 16 (modal h3), 22 (h1).

Additional rules:
- Badge / status indicators: 11px, weight 500–600, `--font-mono`
- File count / timestamp metadata: 12px, weight 400, `--font-body`, color `--muted`
- Rename input: 14px, weight 400 (not 600 while in edit mode), `--font-body` — text pre-selected on mount

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#faf9f7` via `--bg` | Page background, card background, input backgrounds, modal background |
| Secondary (30%) | `#ffffff` via `--card` + `#e5e2dc` via `--border` | Card surfaces, borders, dividers, hover row tints |
| Accent (10%) | `#2563eb` via `--accent` | Reserved list below |
| Destructive | `#dc2626` (hard-coded, not a variable) | Delete confirmation button background, delete button border |

Source: `src/App.jsx` lines 1450–1458 + existing modal pattern line 1586.

**Accent reserved for:**
- Save button background + border (`--accent` fill)
- "Show"/"Hide" toggle text links in ProjectList header
- Active project "active" badge background (`--accent-bg`) and text (`--accent`)
- Drag-over drop zone border
- Focus ring outline (`#2563eb` via `index.css :focus-visible`)
- Server status dot when server is connected: `#10b981` (green — not accent, semantic-only)

**NOT accent:**
- Cancel buttons (use `--bg` + `--border`)
- Hover icon buttons for rename/delete (use `--bg` + `--border`, NOT `--accent`)
- Modal overlays (use `rgba(0,0,0,0.5)`)
- Server status dot when offline: `#9ca3af` (gray — semantic-only)

**New semantic colors this phase (hard-code, not variables):**
- Server connected dot: `#10b981` (emerald-500)
- Server offline dot: `#9ca3af` (gray-400)
- Destructive button: `#dc2626` (red-600) — already established in existing modal

---

## Component Contracts

### 1. Hover Action Buttons (pencil + trash)

- Trigger: `hovered === project.id && editingId !== project.id` — hide during inline rename
- Placement: right side of project card, inside the existing `justifyContent: "space-between"` flex row
- Size: 28×28px, `borderRadius: 4`, `border: "1px solid var(--border)"`, `background: "var(--bg)"`
- Pencil icon: Unicode `✏` (`&#x270F;`) or inline SVG `<path d="M11 4H4a2 2 0 0 0-2 2v14..."/>` at 14px
- Trash icon: Unicode `🗑` or inline SVG at 14px — MUST NOT use emoji in production; use SVG path
- Pencil: `title="Rename project"`, `aria-label="Rename project"`
- Trash: `title="Delete project"`, `aria-label="Delete project"`, text/icon color `#dc2626` on hover
- Both: `onClick={e => { e.stopPropagation(); ... }}` — prevents card click triggering project switch
- Transition: `all 0.15s ease` (inherited from `index.css button` rule)

### 2. Inline Rename Input

- Replaces the project name `<span>` in the card — same position, same width
- Style: `fontSize: 14, fontWeight: 400, fontFamily: "var(--font-body)", color: "var(--text)", background: "var(--bg)", border: "1px solid var(--accent)", borderRadius: 4, padding: "2px 6px", outline: "none", width: "100%"`
- Accent border (not muted border) signals active editing state
- `autoFocus` + `useEffect(() => editRef.current?.select(), [editingId])` — pre-selects text on mount
- Enter key: confirms rename
- Escape key: cancels (uses `cancelledRef` pattern — see RESEARCH.md Pitfall 2)
- Blur: confirms rename (guarded by `cancelledRef`)
- `onClick={e => e.stopPropagation()}` — prevents card click firing project switch
- Empty name on confirm: reject, keep editing — do not allow blank project names; show inline border color `#dc2626`

### 3. Delete Confirmation Modal

Mirrors existing unsaved-changes modal in `src/App.jsx` lines 1560–1595. Same overlay, same box, same button treatment.

- Overlay: `position: "fixed", inset: 0, background: "rgba(0,0,0,0.5)", zIndex: 1000`
- Box: `background: "var(--bg)", borderRadius: 12, padding: 24, maxWidth: 400, border: "1px solid var(--border)", boxShadow: "0 8px 32px rgba(0,0,0,0.2)"`
- Title (h3): `fontSize: 16, fontWeight: 700, margin: "0 0 8px"`
- Body (p): `fontSize: 13, color: "var(--muted)", margin: "0 0 16px", lineHeight: 1.5`
- Button row: `display: "flex", gap: 8, justifyContent: "flex-end"`
- Cancel button: `padding: "6px 16px", fontSize: 13, border: "1px solid var(--border)", borderRadius: 6, background: "var(--bg)", color: "var(--text)"` — default focus target
- Delete button: `padding: "6px 16px", fontSize: 13, border: "1px solid #dc2626", borderRadius: 6, background: "#dc2626", color: "#fff", fontWeight: 600` — NOT default focus target
- State: `deleteTarget: { id, name, chapters }` as local state in `ProjectList.jsx` — null = modal closed
- Modal placement: bottom of `ProjectList.jsx` return, before closing `</div>`

### 4. Server Status Dot

- Placement: inside existing `<div style={{ display: "flex", alignItems: "center", gap: 8 }}>` in the header (App.jsx line 1474), to the LEFT of the save-state badge
- Size: `width: 8, height: 8, borderRadius: "50%", display: "inline-block"`
- Connected: `background: "#10b981"` (green)
- Offline: `background: "#9ca3af"` (gray)
- Cursor: `"default"` — not interactive, tooltip only
- Tooltip: native HTML `title` attribute — no custom tooltip component
- No animation, no pulsing — static dot

---

## Interaction States

| Component | State | Visual Treatment |
|-----------|-------|-----------------|
| Project card | Default | `background: "var(--bg)"` |
| Project card | Hovered (inactive) | `background: "var(--accent-dim)"` + show pencil/trash icons |
| Project card | Active | `background: "var(--accent-bg)"` + "active" badge, no hover icons visible |
| Project card | Rename editing | Name span replaced with bordered input; hover icons hidden |
| Rename input | Valid | `border: "1px solid var(--accent)"` |
| Rename input | Invalid (empty) | `border: "1px solid #dc2626"` |
| Hover icon: trash | Hovered | icon color shifts to `#dc2626` |
| Server dot | Connected | `#10b981` green |
| Server dot | Offline | `#9ca3af` gray |
| Delete modal | Open | Full-screen backdrop, box centered, Cancel is default |
| Delete modal | Pending delete | No loading state — operation is instant from user's perspective |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — rename confirm | Enter key or blur — no button label needed (keyboard-driven) |
| Primary CTA — delete trigger | "Delete" (trash icon button, accessible via `aria-label="Delete project"`) |
| Delete modal title | "Delete Project?" |
| Delete modal body | "Delete **{project.name}**? This project has {N} file{s} and cannot be recovered." |
| Delete modal — warning note | "This cannot be undone." (separate line, same muted color) |
| Delete modal confirm button | "Delete Project" |
| Delete modal cancel button | "Cancel" |
| Rename input placeholder | (none — input is pre-seeded with current project name, no placeholder needed) |
| Empty rename — validation | (no toast — simply retain red border and keep focus; do not submit) |
| Server dot — connected tooltip | "Server connected — projects backed to disk" |
| Server dot — offline tooltip | "Server offline — saving to browser only" |
| Existing empty state (unchanged) | "No saved projects yet. Save your current workspace to get started." |

Source: Delete modal copy — informed by CONTEXT.md "Modal shows project name, file count, and 'This cannot be undone' warning".
Server dot tooltip — verbatim from CONTEXT.md.

**Tone rules for this phase:**
- Direct, no ellipsis on button labels ("Delete Project" not "Deleting...")
- File count uses singular/plural: "1 file" / "3 files"
- No exclamation points in destructive contexts

---

## Animation and Motion

All values consistent with existing `index.css` and App.jsx transitions:

| Element | Animation |
|---------|-----------|
| Hover icon appearance | `opacity: 0 → 1` over 0.15s via `transition: all 0.15s ease` |
| Hover icon disappearance | Same, reverse |
| Card hover background | `transition: "background 0.1s ease"` — already set in `ProjectList.jsx` |
| Delete modal entry | No animation — consistent with existing unsaved-changes modal (none) |
| Rename input mount | No transition — direct replacement; `autoFocus` causes browser-native focus transition |

Do not add new animation keyframes unless they match the existing `fadeSlideIn` or `fadeIn` patterns from `index.css`.

---

## Accessibility

| Requirement | Implementation |
|-------------|---------------|
| Hover icons keyboard accessible | Buttons are native `<button>` elements — tab-focusable, Enter/Space activates |
| Hover icons focusable without hover | Icons visible when button receives `:focus-visible` even if mouse not over card |
| Rename input `aria-label` | `aria-label="Rename project: {project.name}"` |
| Delete button `aria-label` | `aria-label="Delete project: {project.name}"` |
| Modal focus trap | Cancel button receives focus on modal open (`autoFocus` on Cancel) |
| Escape closes modal | `onKeyDown` on modal container: Escape → close |
| Server dot `aria-hidden` | `aria-hidden="true"` — decorative only; screen reader information via tooltip text on a sibling element if needed |
| Focus ring | Inherited from `index.css :focus-visible` — `outline: 2px solid #2563eb; outline-offset: 2px` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (shadcn not initialized) |
| third-party | none | not applicable |

No third-party component registries. All new UI built from native HTML elements and CSS variables consistent with the existing codebase pattern.

---

## What Is NOT Changing in This Phase

The following existing UI elements are explicitly out of scope and must not be modified:

- Header project name input (`placeholder="Project name..."`)
- Save button label ("Save")
- New button label ("New")
- Save-state badge (Saved / Saving... / Unsaved) — appearance unchanged; server dot adds to its right-side flex row
- ProjectList collapsed state and "Show"/"Hide" toggle
- Unsaved-changes modal (used for project switching — do not merge with delete modal)
- BookMeta, UploadZone, ConvertButton, chapter row components — untouched

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
